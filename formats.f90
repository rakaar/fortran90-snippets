program test
    
    ia = 1234
    ib = 12345



end program test

! formats

! use x for spaces

! integer - width of integer = num of digits
! write(*, "(i4,i4)")
! write(*, "2(i4)") ! both are same
! write(*, "2(i4, 2x)") ! 2x is space

! real 
! write(*, "(f10.3)") ! 123.45 # 3 places after decimal 
! f10 => 10 =  places_before_decimal + places_after_decimal
! exp format "e10.3"
! 3f10 => 3 numbers

! character
! a8 => 8 characters
!a5 => is equialent to a(1:6)


